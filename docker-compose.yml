services:
  dn42-portal:
    build: .
    container_name: dnpeering
    restart: unless-stopped
    ports:
      - "8080:80"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=80
